{
    "variables": {
        "revision": "{{env `GO_REVISION`}}",
        "pipeline_counter": "{{env `GO_PIPELINE_COUNTER`}}"
    },
    "builders": [{
        "type": "digitalocean",
        "region": "lon1",
        "image": "ubuntu-14-04-x64",
        "snapshot_name": "disk-image-demo-{{user `pipeline_counter`}}.{{user `revision`}}",
        "state_timeout": "60m"
    }],
    "provisioners": [{
        "type": "shell",
        "inline": ["echo foo", "apt-get install sl"]
    }]
}

